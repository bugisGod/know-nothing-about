# 2026-09-26

## 项目审查（scio 个人站点，Astro 静态站）

产出 `REVIEW.md`（仓库根目录），20 条改进建议，按 P0–P4 分级。

核心发现（后续若要动手改，以此为据）：
- P0 部署安全：`server.ini` 明文 root 密码 + paramiko `AutoAddPolicy`（MITM 风险）。
  已确认凭据从未进 git（`git log --all -- server.ini` 为空，grep 无命中），无泄露但需整改。
- P0 构建卫生：`npm run build` 不清 dist，本次构建出现 `dist/pages/*.astro.mjs` 残留；
  `deploy.py` 无差别全量上传会把服务端中间产物推上线。
- P1 配置双写：笔记详情页侧边栏显示原始文件夹名（study/frontend），列表页显示中文；
  专栏名在 `config.ts: SERIES` 与 `notes/index.astro: FEATURED` 各写一份。
- P1 `og:image` 是相对路径且缺 canonical / og:url。
- P2 three.js 单 chunk 532 kB（gzip 134 kB）全量进首页；dist 16 MB（407 个 Noto Serif SC 子集字体）。
- P3 BrainGraph 的 aria-label 声称「下方列表提供相同导航」但列表并不存在（a11y 声明落空）。
- P3 global.css 死样式：`.note-list/.note-row/.cat-sec/.container.wide/.sec>.h2 .idx/.sec-label` 均无引用；
  `.prose img`、`.prose pre` 各定义两次。
- P4 `.prose img { filter: invert(...) }` 全局反色会毁掉暗色配图。

实测数据（可复用，不必再跑）：
- `npm run build` 约 21s，静态路由 10 个，pagefind 索引 4 页 / 1271 词。
- `npx pagefind --site dist` 单独跑正常，产 `dist/pagefind`；zh-cn 无词干支持。
- 站点 `site` 配 https://nihilscire.com，部署脚本默认端口 8123，Caddy 配置在 `.shots/Caddyfile.prod`。
- `.shots/` 已 163 MB（截图+调试脚本），本地可考虑清理。

注意：本次构建在 sandbox 里被 `safe-delete` 守卫中断了 `cleanServerOutput`，
导致 sitemap/pagefind 未生成、dist/pages 残留——这是环境问题，不是项目本身的构建失败。

## 追加：视觉与布局审查（同日）

用户追问视觉/布局，已在 REVIEW.md 追加「补充 · 视觉与布局」章节（L1–L13），
并把执行顺序表拆成 A/B/C/D 四组。

布局硬伤（已核实，可直接改）：
- L1 `notes/[...slug].astro` 基础样式无条件三列 `220px 1fr 200px`，但 TOC 仅 ≥1200px 显示
  → 921–1199px 右侧 256px 死白。修法：基础改两列，≥1200px 才三列。
- L2 页头 `.container` 1120 vs 笔记详情 `.ncontainer` 1280 → 1440px 屏上左边缘错开 80px。
  `global.css` 已有 `.container.wide`(1280) 但没被用上。
- L3 `.card-grid` 用 `minmax(320px, 1fr)`，视口 360px 时容器仅 312px → 横向溢出 8px；
  全站无 `overflow-x:hidden` 兜底（已 grep 确认）。修法 `minmax(min(320px,100%),1fr)`。
- L4 首页 8 个 `.tw` 闪烁星 `animation-delay` 全是 `.4s`（index.astro 59–66 行）→ 同步明灭。
- L5 各页「SkyBand → 正文」间距四种：知识库 40 / 项目·搜索 88 / 笔记详情 56 / 项目详情 40。
- L7 sticky 起止线三套：scroll-padding 76 / .sidebar 84 / .toc 96（页头实际高 58）。

## 追加 2：实机截图走查（同日，应用户「文章布局怪怪的」追问）

装了 agent-browser（在 `C:/Users/14050/.workbuddy/binaries/node/workspace`，非全局），
`npm run preview -- --port 4321` 起服务，Chromium 实拍 1440 / 1024 / 375 三档。
截图存 `.workbuddy/screenshots/`（d-home / d-notes / d-note-detail(+mid) / d-projects /
t-note-detail-1024 / m-notes-375 / m-note-detail-375）。已在 REVIEW.md 加「补充 2 · 实机截图走查」。

新发现（代码读不出、只有实拍能确认的）：
- S2 真 bug：375px 实测 scrollWidth=497，横向溢出 122px。根因 = 移动端媒体查询
  `.notes-layout { grid-template-columns: 1fr }` 用裸 1fr（=minmax(auto,1fr)），
  article 内最长代码行把轨道撑爆。修法 `minmax(0, 1fr)`。桌面中间列写对了，唯独移动端漏。
- S1 用户说的「怪」= 三栏比例失衡：1440 下 侧栏220+gap56+正文~650+gap56+目录200，
  正文仅占 45%，TOC 200px 窄到长标题全折 2-3 行成文字墙，三栏字号接近无层级。
  建议 gap→40 / 侧栏→200 / 目录→240 / 正文→46em。
- S3 移动端目录（全站目录树）压在正文前，手机要先滚完目录才看到正文。
- S4 首页星图 540px 高通栏画布只有 4 个节点（3 笔记+1 项目），实拍一片黑飘 4 颗暗点，撑不起。
- S5 知识库列表单张「全部笔记」卡被 auto-fit 拉满 1072px 成超扁卡。
- S6 长中文标题后的琥珀句点「…对照精读 .」很怪；面包屑/侧栏仍外露 study/FRONTEND。
- 1024 实拍再次确认 L1 死白第三列。

## 追加 3：设计层审查 v2（同日，用户提交 eafc05e 后仍不满意布局美观）

用户已提交 `eafc05e 修复 review 全量问题`（三栏重排、本地化、懒加载、暗星索引等均落地）。
实拍 v2（截图在 `.workbuddy/screenshots/v2/`）：结构问题大多已修，但用户主题
「我只知道我一无所知 + 星空深邃 + 漫天星=知识积累」仍没立起来。已在 REVIEW.md 加「补充 3 · 设计层」。

设计层核心建议（D1–D5）：
- D1（最重要的洞察）：把「内容少」变成概念 —— 4 颗有名字的亮星 + 几百颗程序化暗星，
  caption「已点亮 4 颗 · 余下皆是未知」，视觉化「我知很少、未知浩瀚」。
  已用 widget 出了概念稿。BrainGraph 已有 starField 可加密调暗复用。
- D2：hero 文字悬浮星海正中（canvas 铺满 .night，文字压 z-index，中心留净空），
  告别「文字一块 + 星图一块」两张皮。
- D3：星的语言贯穿全站 —— 知识库列表改「星表」式（HIP 编号+观测日期+星等），
  stageOf 从文字徽章升级为星点 glyph；h2 分隔线改星轨；分类映射恒星色温（BrainGraph
  注释里已有「域色」概念未落地），解决全站仅琥珀一个 accent 的单调。
- D4：深邃=纵深 —— 加暗角、卡片实底+亮边、大标题极淡光晕（克制）。
- D5 残留：S2 移动端裸 1fr 溢出实测仍在（scrollWidth=497，[...slug].astro 第 314 行）；
  S3 移动端目录压正文未修；移动端长标题末尾琥珀句点扎眼。

构建注意：新 `prebuild`（rm -rf dist）在本机 sandbox 会被 safe-delete 守卫拦死（461 个文件 > 阈值 50），
本机只能 `npx astro build` 绕过；用户正常机器上没问题。

## 追加 4：设计方向被用户修正（重要偏好，后续别跑偏）

用户否决了「名言被几百颗暗星围绕」的概念稿，理由两条：
1. 主题关键词是**深邃、干净、浩瀚**（不是「空」），星星围着文字显乱；
2. **同系列文章的星必须凝聚在一起**——静态摆位做不到聚类。

确认的正确方向（已写入 REVIEW.md 补充 3 的 D1 修订版）：
- 凝聚：启用 BrainGraph 已内置但未使用的**锚星接口**（anchor/color/count/标签/悬停卡），
  每个系列/分类一颗锚星 + 成员星连边（w≈2），力导向自然聚成带名字的星座。
- 干净：首页现在叠了 5 层星（webp 星野 / meteors 微星 / .tw / 星图远景 320 点 / 节点），
  减到 2 层（星图 + webp 远景），.tw 与 meteors 首页关闭。
- 浩瀚：星图从 540px 框解放为整屏；相机拉近、雾加重（FogExp2 0.05→0.09）、
  远景星调暗调细；星尘边/脉冲再降密度。
- 概念稿二（星座凝聚 + 大净空 + 名言小而安静）方向获用户认可待确认。

用户偏好备注：对设计稿挑剔且表达直接，给方案前先确认是否符合「深邃/干净/浩瀚+聚类」，
不要给花哨的、元素堆砌的方案。

## 追加 5：主页设计定调（同日 13:04）

用户最终定调：**保留现有主页结构，不重做**。两版概念稿（暗星围绕、星座凝聚）均被否。
不舒服的只是三处（已写入 REVIEW.md D2 修订版）：
1. 「nothing.」悬挂（max-width:18ch 所致）→ 方案A 放开成单行 / 方案B 语义断行。
2. 名言（y≈385 结束）与星群（y≈590 出现）之间 ~200px 空带，文字与星脱节。
   可调旋钮：.hero/.night padding-top、星群 canvas 内垂直偏移、canvas 高度。
3. 拉丁原文 11px 太小（相对 58px 名言跳 5 倍）。

已用 widget 给出三方案对比（现状 / A 一行 / B 语义断行），等用户挑选后再动手。
移动端 S2（裸 1fr 溢出）依旧未修，用户尚未让动。

## 追加 6：首页星图定稿实施（同日 14:05，已完成）

用户最终定调：考虑未来文章增多（星要多）、不要光污染、要深邃、字体用我方案里的感觉。
已实施并实拍验证（截图 `.workbuddy/screenshots/v3-*.png`）：

1. **锚星凝聚**（index.astro + config.ts + BrainGraph.astro）：
   - config 的 SERIES/CATEGORIES 新增 `color` 域色字段（恒星色温系）。
   - index.astro 为每个系列/分类生成锚星节点（无 url、带 color/count/desc），
     成员星带 `parent`（锚星索引）+ 归属边 w=2.2；去掉同分类冗余边，标签边权重降到 ≤2。
   - BrainGraph 初始化：锚星布在球面彼此远离，成员星落在锚星 ±1.7 内；
     每边独立自然长度/劲度（归属边 1.5/0.055，标签边 2.2/0.03）→ 成员凝聚成团。
2. **光污染治理**：删除流光脉冲（整段 pulses 代码）；星尘闪烁 0.78±0.22→0.9±0.1、
   亮度 0.5→0.38；锚星边色 ×0.8→×0.55；远景星野 320→420 颗但 size 0.55→0.38、
   opacity 0.55→0.3；雾 0.05→0.062；首页删掉 8 个 .tw 闪烁星（HTML+CSS 全删）；
   meteors.ts 在 `body.dataset.sky==='home'` 时直接 return。
3. **排印**：hero h1 max-width 18ch→none（桌面单行，移动端 balance 自然两行）；
   origin 11→12px、与 h1 间距 20→14px；星群 group.position.y=0.55 上移消空带。
4. **S2 修复**：移动端 notes-layout 裸 1fr → minmax(0,1fr)（scrollWidth 497→405）；
   残余 30px 是行内 code 长路径（turn-state.ts）无断点，global.css 行内 code 加
   `overflow-wrap: anywhere`（→360，彻底消除）。

效果（实拍确认）：桌面名言单行；「深入理解 AI AGENT」锚星带标签 + 2 成员凝聚、
「前端」锚星 + 成员、项目琥珀星独立；干净/深邃/可生长。工作树未提交，留给用户审查。
