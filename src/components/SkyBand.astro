---
interface Props {
  compact?: boolean;
  reading?: boolean;
}
const { compact = false, reading = false } = Astro.props;
---

<header class:list={["sky-band", { compact, reading }]}>
  <div class="container">
    <slot />
  </div>
</header>
<style>
  .sky-band {
    padding: 68px 0 42px;
  }
  .sky-band.compact {
    padding-top: 48px;
  }
  .sky-band.reading .container {
    max-width: 1040px;
  }
  .sky-band :global(h1) {
    max-width: 740px;
    font-family: var(--font-display);
    font-size: clamp(30px, 3.4vw, 44px);
    font-weight: 400;
    line-height: 1.4;
    margin: 14px 0 18px;
    letter-spacing: 0;
    text-wrap: balance;
  }
  .sky-band :global(.page-desc) {
    margin-bottom: 0;
  }
  @media (max-width: 760px) {
    .sky-band,
    .sky-band.compact {
      padding: 32px 0 24px;
    }
    .sky-band :global(h1) {
      font-size: 28px;
      text-wrap: initial;
    }
  }
</style>
