# 项目审查报告 · scio 个人站点

> 审查时间：2026-09-26 · 范围：全量源码 + 构建产物 + 部署链路
> 结论：项目完成度高、审美统一、代码质量不错（无障碍与性能细节考虑得比多数个人站好）。
> 主要风险集中在 **部署链路**、**three.js 体积**、**配置双写不一致** 三处。

## 总体评分

| 维度 | 评价 | 说明 |
| --- | --- | --- |
| 代码组织 | 良好 | 组件拆分清晰，config 集中，schema 有类型约束 |
| 视觉设计 | 优秀 | 星空主题贯穿到底，CSS 变量体系完整 |
| 无障碍 | 中等 | 有 reduced-motion / aria 意识，但图谱无键盘可达路径 |
| 性能 | 偏弱 | 首页 532 kB JS（three.js）+ 16 MB 产物 |
| 部署运维 | 偏弱 | 全量覆盖式上传、无回滚、凭据明文 |
| 文档一致性 | 偏弱 | README 与实际部署方式不符 |

---

## P0 · 需要尽快处理

### 1. 部署凭据以明文 root 密码形式存放，且用 root 直连 + 自动接受主机指纹

`server.ini` 里是 `IP / PWD / PORT` 三件套，`deploy.py` 用
`paramiko.AutoAddPolicy()` + `username='root'` + 密码登录。

好消息：已确认 `server.ini` **从未进入过 git 历史**（`git log --all -- server.ini` 为空），
仓库里也 grep 不到密码和 IP。所以目前没有泄露，但风险敞口仍在：

- `AutoAddPolicy` 会无条件信任任意主机指纹 → 中间人攻击可直接拿到 root 密码
- 用 root 跑部署，一次误操作就是整台机器
- 密码明文落盘，备份/同步/截图都可能外泄

**建议**（按性价比排序）：
1. 换 SSH key 登录，`.ssh/config` 里固定主机；`load_system_host_keys()` + `RejectPolicy`
2. 新建专用部署用户（如 `deploy`），只给 `/var/www/scio` 写权限
3. 凭据改走环境变量（`SSHPASS` / `DEPLOY_KEY`），`server.ini` 只留 IP 和端口
4. 顺手把服务器 root 密码改一遍（既然已经在明文文件里躺过）

### 2. `dist/` 构建前不清空，残留文件会被推上线

本次我本地执行 `npm run build`，产物里出现了 `dist/pages/*.astro.mjs`（服务端中间产物残留），
而 `deploy.py` 是**无差别遍历上传**——这些 `.mjs` 会被推到 `/var/www/scio/pages/` 并对外可访问。
同时 sitemap / pagefind 索引这次没生成成功，`&&` 链就断了。

**建议**：
- `build` 脚本改为 `rimraf dist && astro build && pagefind --site dist`
- `deploy.py` 上传前校验关键产物存在（`dist/index.html`、`dist/pagefind/`、`dist/sitemap-index.xml`），缺失即中止
- 上传时跳过 `dist/pages/`、`dist/**/*.mjs`

---

## P1 · 功能与一致性缺陷（有明确 Bug）

### 3. 笔记详情页侧边栏分类名未本地化

`src/pages/notes/[...slug].astro:68` 直接输出文件夹名：

```
<div class="cat">{cat}</div>          →  显示 "study" / "frontend"
```

而列表页 `notes/index.astro` 用了 `catLabel()` → 显示 "学习笔记" / "前端"。
同一个分类在两处显示两种名字。

**修法**：`[...slug].astro` 引入 `CATEGORIES`，复用同一个 `catLabel()`；
最好把 `catOf` / `catLabel` / `fmtDate` 抽到 `src/lib/content.ts` 统一导出（现在三个页面各写一份）。

### 4. 专栏名称在两处各写一遍，改配置不生效

`config.ts` 定义了 `SERIES['ai-agent-book'].title`（被详情页使用），
但 `notes/index.astro:17` 又硬编码了一份：

```ts
const FEATURED = { id: 'ai-agent-book', title: '深入理解 AI Agent' };
```

在 config 里改名字，列表页不会跟着变。

**修法**：列表页改为读 `SERIES`，并支持多个专栏（现在只有单个 featured 位是硬编码结构）。

### 5. `og:image` 用相对路径，社交分享预览会失效

`Base.astro:33` 是 `content="/og.png"`。Facebook / X / 微信抓取需要**绝对 URL**。
同时缺 `og:url` 和 `<link rel="canonical">`——后者对「同一篇文章可能被多个 URL 命中」的 SEO 很重要。

**修法**：

```astro
const canonical = new URL(Astro.url.pathname, Astro.site);
<link rel="canonical" href={canonical.href} />
<meta property="og:url" content={canonical.href} />
<meta property="og:image" content={new URL('/og.png', Astro.site).href} />
```

### 6. 构建期 `Date.now()` / `new Date()` 导致内容「冻龄」

- `Footer.astro:5` —— `new Date().getFullYear()` 在**构建时**求值。2027 年不重新构建，页脚就一直是 2026。
- `notes/index.astro:22` `stageOf()` —— 用 `Date.now()` 算文章年龄，"新星 → 亮星 → 恒星" 标签在构建那一刻定格，之后不再变化。

**修法**：页脚年份这类可以接受（反正每年都会发文章重新构建）；但 `stageOf` 建议改成纯 frontmatter 字段或干脆去掉——它现在唯一的作用是给读者看一个会过期的标签，收益低维护成本为正。

### 7. 全局图片强制反色，会毁掉暗色配图

`global.css:741`：

```css
.prose img { filter: invert(0.92) hue-rotate(180deg); }
```

规则的本意是「把书里的白底线稿反色融入夜空」，但它作用于**所有**正文图片。
一旦你贴一张暗色截图、终端输出或照片，就会被反色成刺眼的亮块。

**修法**：改成 opt-in，比如只反色带 `?invert` 或 class `figure-line` 的图，
或者用 Markdown 图片 title 语法触发；白底 SVG 直接改源文件颜色最省事。

---

## P2 · 性能

### 8. three.js 让首页背了 532 kB（gzip 134 kB）JS

构建输出：

```
dist/_astro/BrainGraph...js  532.83 kB │ gzip: 134.06 kB
```

星图只在首屏出现，但脚本是**同步全量加载**的。移动端 + 弱网下首屏会被明显拖慢，
而首页除星图外其实只有一句话文案。

**建议**（从易到难）：
1. 把 `<script>` 改成 `IntersectionObserver` 触发的 `await import('three')`，滚到才加载
2. 窄屏（`< 768px`）或 `navigator.hardwareConcurrency <= 4` 时降级：不加载 three，直接渲染一份静态的文章链接列表（顺便解决第 11 条的无障碍问题）
3. 首帧先用一张静态预览图占位，避免白屏

### 9. 产物 16 MB，其中 407 个字体文件

`dist/_astro` 16 MB，字体 407 个（Noto Serif SC 按 unicode-range 切成了几百个子集）。
浏览器虽然按需下载，但：

- `deploy.py` 每次全量上传，16 MB 全走一遍 SFTP
- 服务器上堆几百个永远不会被多数访客用到的文件

**建议**：中文字重只保留 400/600 里真正用到的（标题用 Instrument Serif + 系统中文衬线兜底即可）；
正文中文直接用系统字体栈（PingFang SC / 微软雅黑 / Noto Sans SC），
去掉 Noto Serif SC 的体积能砍掉一个数量级。

### 10. 部署无增量、无清理、无回滚

`deploy.py` 对每个文件无条件 `sftp.put`，且远端从不删除。后果：

- 改一次构建就要传 16 MB
- 文章改名 / 图片替换后，旧文件在服务器上永久残留（还占 Caddy 目录）
- 上传中途断网 → 线上是半新半旧的混合状态
- 没有回滚手段

**建议**：
- 上传到本地暂存目录 → 远端先 `mv` 旧版本到 `scio.bak`，新版本就位后切换软链（`current -> scio-20260926-1200`），失败则软链指回
- 或直接用 `rsync -a --delete dist/ deploy@host:/var/www/scio/`（比 Python SFTP 快且自带清理）
- 保留最近 3 个版本用于回滚

---

## P3 · 可维护性

### 11. 图谱的 aria-label 承诺了不存在的替代内容

`BrainGraph.astro:36`：

```
aria-label="三维知识图谱：每个节点对应一篇文章，连线表示共享标签。下方列表提供相同的导航。"
```

但下方只有一句 `拖拽 · 点击星体探索`，**没有任何列表**。键盘用户和读屏用户完全拿不到图谱里的信息。

**修法**：在图谱下方补一份 `visually-hidden`（或 `<details>` 折叠）的文章链接列表，
内容和节点一一对应。这样 a11y 声明才成立，也顺手解决了第 8 条的降级需求。

### 12. global.css 里有一批已无引用的死样式

grep 全量确认，**以下选择器在当前任何页面都不再使用**：

- `.note-list` / `.note-row`（含其 `@media (max-width:640px)` 分支）
- `.cat-sec`（含 `.cat-label` / `.count` 及其移动端分支）
- `.container.wide`（`[...slug].astro` 自己另写了 `.ncontainer`）
- `.sec > h2 .sec-label` / `.idx`

另外 `.prose img` 和 `.prose pre` 各被定义了两次（分散在文件两处），后续改样式容易改错地方。

**建议**：清掉死代码，合并重复规则。810 行的 global.css 能瘦身两成左右。

### 13. 首页组件里直接改全局样式

`index.astro` 用 `:global(main.site-main)` 和 `:global(.site-footer)` 修改了别的组件的样式。
首页一改，全站页脚跟着变，排查时很难定位。

**修法**：这类跨组件覆盖收回 `global.css`，或改成页面级 wrapper class。

### 14. 依赖分类有误

`@types/three` 放在 `dependencies`（包体积和运行时都不需要），应移到 `devDependencies`。

### 15. README 与实际部署方式脱节

README 讲 Vercel / Netlify / GitHub Pages，实际是自建服务器 + Caddy（配置躺在 `.shots/Caddyfile.prod`）。
新人（包括半年后的你）会照 README 操作然后白忙。另外 `.shots/` 已 163 MB（截图 + 调试脚本），
虽已 gitignore 但本地占空间，建议定期清理或挪出仓库目录。

**建议**：README 补一节「自建服务器部署」；正式 Caddy 配置从 `.shots/` 提到仓库内 `deploy/Caddyfile`。

### 16. 没有 CI

现在发一篇文章要手动 `npm run build` + `python deploy.py`。建议在 GitHub Actions 里做：
push 到 main → 构建 → pagefind → rsync 到服务器。省掉每次手动跑，也避免忘记跑 pagefind 导致搜索失效。

---

## P4 · 体验细节

### 17. 404 页主视觉对比度不达标

`.code` 用 `--border-strong`（#3a3d55）在 `--bg`（#0b0c12）上，实测对比度约 **1.8:1**，
远低于 WCAG AA 要求的 3:1（大字号）。视觉上「故意低调」和「看不见」是两回事。

### 18. Pagefind 对中文无词干支持

构建输出明确提示：

```
Note: Pagefind doesn't support stemming for the language zh-cn.
```

中文检索退化为子串匹配，用户搜「智能体是如何规划的」这种长句基本召回不到。
建议：给笔记加 `data-pagefind-meta="tags"` 提升召回，或在搜索页提示「建议使用简短关键词」。

### 19. 上一篇 / 下一篇的箭头方向反直觉

`[...slug].astro`：左侧是 `← 下一篇`（更旧的），右侧是 `上一篇 →`（更新的）。
读者习惯「向右 = 更新」，现在是反的。建议左旧右新，或直接用日期文案避免歧义。

### 20. 配置里有空分类

`CATEGORIES` 定义了 `tools` / `reading` / `projects` 三个分类，但：
- 前两个一篇笔记都没有
- `projects` 是独立集合，永远不会出现在 notes 里

建议在内容真正长出来前先删掉，避免「分类存在但点进去空」的错觉。

---

## 补充 · 视觉与布局

这一类是「打开页面肉眼可见」的问题，按影响面排序。

### L1 · 笔记详情页右侧有一整条死白（921–1199px）

`[...slug].astro` 的基础样式是无条件三列：

```css
.notes-layout { grid-template-columns: 220px minmax(0,1fr) 200px; gap: 56px; }
```

但 TOC（第三列的内容）只在 **≥1200px** 才 `display:block`，<1200px 时 `display:none`。
结果在 921–1199px（iPad 横屏、小尺寸笔记本）区间，第三列是**空的 200px + 56px gap = 256px 死白**，
正文被挤到偏左，页面重心歪掉。

**修法**：基础样式改成两列 `220px minmax(0,1fr)`，只在 `@media (min-width:1200px)` 里升为三列。

### L2 · 页头与笔记正文左边缘对不齐

- 页头 `Header.astro` 用 `.container` → `max-width: 1120px`
- 笔记详情页用自己写的 `.ncontainer` → `max-width: 1280px`

在 1440px 屏幕上，页头内容从 160px 起、正文从 80px 起，**错开 80px**。
别的页面（知识库/项目/搜索）页头与正文都是 1120，只有笔记详情这一页跑偏。
`global.css` 里其实已经备好了 `.container.wide`（1280）—— 只是没被用上。

**修法**：笔记详情页改用 `.container.wide`，并把 `.ncontainer` 删掉；
或者反过来让页头也走 1280。二选一，关键是**全站只有一条中轴线**。

### L3 · 知识库卡片网格在窄屏横向溢出

```css
.card-grid { grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); }
```

`minmax()` 的 320px 是硬下限。视口 360px 时容器内容宽只有 `360 - 48 = 312px` < 320px →
**溢出 8px**。而全站没有任何 `overflow-x: hidden` 兜底（我已 grep 确认），
所以在 360px 及更窄的安卓机上，知识库页会出现横向滚动条。

**修法**：`minmax(min(320px, 100%), 1fr)`。

### L4 · 首页 8 颗闪烁星同步明灭

`index.astro` 里 8 个 `<i class="tw">` 的 `animation-delay` **全是 `.4s`**（已核对 59–66 行），
`animation: tw 3s ... alternate` 的周期也完全相同。
结果是 8 颗星一起亮、一起灭 —— 像呼吸灯而不像星空，反而比不闪更扎眼。

**修法**：给每颗星错开 delay 与周期，例如
`animation-delay: .4s / 1.1s / 2.3s …`，duration 在 2.4–4.2s 间随机。

### L5 · 各页「页眉带到正文」的间距各不相同

| 页面 | SkyBand 内元素 | 到正文的距离 |
| --- | --- | --- |
| 知识库 | `.sub`（无下边距） | 40px |
| 项目 / 搜索 | `.page-desc`（`margin-bottom: 48px`） | 40 + 48 = **88px** |
| 笔记详情 | `.page-meta`（`margin-bottom: 16px`） | 40 + 16 = 56px |
| 项目详情 | page-meta 在 SkyBand **外面** | 40px |

四种垂直节奏。翻页时能明显感觉到「有的页松、有的页紧」。

**修法**：把 SkyBand 的 `padding-bottom` 与内部元素的下边距统一收进组件，
页与页之间不再各自为政 —— 例如规定「SkyBand 内最后一个元素 margin-bottom 一律 0，
与正文的间距由 SkyBand 的 padding-bottom 统一给 40px」。

### L6 · 卡片在深空背景上几乎看不见边界

`.card` 的背景是 `color-mix(in oklab, #12141d 86%, transparent)`，
底色与页面 `--bg`（#0b0c12）亮度差很小，边框只有 `rgba(232,230,223,0.09)`，
再叠在星野背景上 —— 卡片的边界几乎融进背景，「可点击区域」的暗示非常弱。

更关键的是它同时开了 `backdrop-filter: blur(10px)`：86% 不透明度下模糊**基本看不出来**，
但性能开销是实打实的，移动端多卡片同屏尤其明显。

**修法**：卡片去掉 `backdrop-filter` 改纯色/渐变；底色提到 `#161822` 一档，
或把边框从 0.09 提到 0.14。页头的 `blur(12px)` 保留（那里半透明效果明显）。

### L7 · 三处 sticky 起止线不一致

- `html { scroll-padding-top: 76px }`
- 侧边栏 `.sidebar { top: 84px }`
- TOC `.toc { top: 96px }`
- 页头实际高度 58px

锚点跳转后标题停在 76px，而两侧 sticky 栏停在 84 / 96px —— 三者互相对不齐，
滚动时能看到两条侧栏的顶端不在同一水平线。

**修法**：统一成一个变量（如 `--sticky-top: 76px`），三处共用。

### L8 · 噪点层盖在整站最上层

`body::after` 的颗粒质感层是 `position: fixed; inset: 0; z-index: 9999`。
它 `pointer-events: none` 所以不影响点击，但：

- 叠加在 sticky 页头、tooltip、甚至 `graph-tip` 之上，**小字号（10.5–12px 的 mono）会发糊**
- 全屏 fixed + 高 z-index 会迫使浏览器为整页维持合成层，滚动性能受影响

**修法**：把它并入 `body::before` 同层（z-index: -1，与背景星野一起），
视觉上几乎没有差别，但不再压在内容之上。

### L9 · 404 页没用 SkyBand，像「没设计过的页面」

全站其余页面都用 `SkyBand` 做页眉带，唯独 404 直接 `.container` + `padding: 120px 0`。
跳转过去时视觉语言断掉一截。另外它的 `.code` 对比度只有 1.8:1（见 P4-17）。

**修法**：404 也套一层 SkyBand（可以用 `compact`），把「404」当页眉标题处理。

### L10 · 首页信息密度过低

首页现在只有一句 tagline + 星图，滚动一屏到底。而 `SITE.bio`、社交链接、最新笔记、
最新项目这些数据都存在，却全部不在首页出现（bio 只在关于页，社交只在页脚）。

对「个人名片 + 知识库 + 项目复盘」的定位来说，首页只承担了「一句名言」。
建议星图下方补一条「最近更新」（3–4 条笔记/项目链接）+ 一句自我介绍，
既给星图之外的访客（和爬虫）一个落地路径，也让首屏不至于空旷。

### L11 · 专栏卡底部留白被吃掉

`.column-card { padding: 22px 26px 8px }` —— 上 22px、下 8px，不对称。
配合最后一行 `.cc-row` 去掉了 `border-bottom`，卡片底边看起来像被裁掉一截。

**修法**：底部 padding 提到 16–18px，与顶部呼应。

### L12 · 正文行高对中文偏松

`.prose { font-size: 15.5px; line-height: 1.95 }` → 行距 30px。
中文舒适区间是 1.7–1.8，1.95 会让段落显得散。同时 `.prose h3`（15.5px）
与正文**同字号**，只靠 600 字重区分层级 —— h2(19px) 到 h3(15.5px) 的跳变也不够明显。

**修法**：中文正文降到 1.8；h3 给到 16.5px 或用 `--text-2` 之外的颜色做层级区分。

### L13 · 竖屏下星图星座可能被裁切

`body::before` 用 `background-size: cover` + 每套星图不同的 `background-position: X%`。
在竖屏手机上 `cover` 会按高度撑满、横向大幅裁切，预设的 X% 定位容易失准，
「今日星座」可能整个被裁掉 —— 而它正是这套背景的主角。

**修法**：竖屏（`(orientation: portrait)` 或 `aspect-ratio < 0.75`）改用
`background-size: auto 100%` 或按宽高比微调 `background-position`。

---

## 补充 2 · 实机截图走查（启动 preview 后用 Chromium 实拍）

上一节是读代码推断，这一节是**实际渲染后的所见**。截图存在 `.workbuddy/screenshots/`。
用户反馈「知识库里的文章布局怪怪的」—— 实拍确认了，而且找到了根因。

### S1 · 笔记详情页的「怪」= 三栏比例失衡（实拍确认）

1440px 桌面下，这一页的空间分配是：

```
侧边栏 220px ┃ gap 56px ┃ 正文 ~650px ┃ gap 56px ┃ 目录 200px
└────────── 非正文 532px ──────────┘   正文只占中间约 45%
```

实拍看到三个叠加的问题：

1. **正文被挤成中间一条窄带**，两侧留出大面积深色空隙 —— 1440px 的屏，正文实际可用只有约 650px。
2. **TOC（右侧目录）200px 太窄**，长标题全部折成 2–3 行：
   「第 1 站 · ReAct 循环的真实形状：一台 10 相位状态机」折 3 行，
   整列 TOC 变成一堵**无法扫读的文字墙**，完全失去「目录」的意义。
3. 侧边栏的笔记标题同样折 2–3 行。三栏字号又接近（13.5 / 15.5 / 12.5px），
   视觉上像**三条互相竞争的文字柱**，没有明确层级 —— 这就是「怪」的来源。

**修法**（核心是重新分配三栏，而不是微调）：
- gap 56px → 40px，把省下的 32px 还给正文
- TOC 200px → 240px，并限制只收 h2（长 h3 折行最乱）；或正文较窄时把 TOC 收进文章开头
- 侧边栏 220px → 200px，长标题允许 `text-overflow: ellipsis` 单行截断，不要折行
- 正文 42em → 46em（中文舒适区）
- 目标比例：`200 ┃ 40 ┃ ~756 ┃ 40 ┃ 244`，正文占比从 45% 提到 60%

### S2 · 移动端笔记详情横向溢出 122px（实测，真 bug）

375px 实测 `document.documentElement.scrollWidth = 497`，**整页横向溢出 122px**，
正文文字在右侧被整列裁掉。根因不是某个长字符串，而是网格：

```css
@media (max-width: 920px) {
  .notes-layout { grid-template-columns: 1fr; }   /* ← 问题在这 */
}
```

裸 `1fr` 等价于 `minmax(auto, 1fr)`，轨道的最小宽度 = 内容的 min-content，
也就是 article 里**最长那行代码**（约 497px）。于是整条轨道被撑到 497px，页面横向滚动。

桌面版中间列写的是 `minmax(0, 1fr)`（对的），唯独移动端这个媒体查询漏了 `minmax(0, …)`。

**修法**：移动端也改成 `grid-template-columns: minmax(0, 1fr);`。
（顺手给 `.prose` 加 `overflow-wrap: anywhere` 作为双保险。）

### S3 · 移动端目录压在正文前面

≤920px 时三栏变单列，DOM 顺序是「侧边栏目录 → 正文 → TOC」。
手机打开一篇文章，**要先滚过整棵全站目录树**（FRONTEND / STUDY 全分组）才能看到正文。

**修法**：移动端把侧边栏默认折叠（`<details>` 或抽屉），或移到正文之后。

### S4 · 首页星图「主角」几乎看不见（实拍确认）

首页那句名言排版很好，但下方 540px 高、通栏宽的 3D 星图里**只有 4 个节点**
（3 篇笔记 + 1 个项目），实拍是一大片黑暗里飘着 4 颗很小的暗点。
作为首屏唯一的「内容」，它撑不起 540px 的高度，显得空。

**修法**（二选一）：
- 内容长起来之前，把星图高度降到 ~320px，并在它下方补一条「最近更新」列表（呼应 L10）
- 或给星图加「分类锚星 + 远景星野」的密度，让 4 个节点也显得有结构

### S5 · 知识库列表「单张卡撑满整行」

`auto-fit + minmax(320px, 1fr)` 在只有 1 张「全部笔记」卡时，这张卡会被拉成
1072px 宽的超扁卡（实拍确认）。内容少的时候网格显得很怪。

**修法**：给卡片加 `max-width`（如 560px），或在篇数少时回退为列表式布局。

### S6 · 实拍确认的其他小问题

- 长中文标题后的琥珀句点：「把书读进源码：ZCode 六站对照精读 **.** 」——
  短标题（知识库. / 项目复盘.）上很点睛，长标题上就像多了个错别字。
  建议只对短标题（如 ≤6 字）加句点，或干脆去掉 `h1::after`。
- 面包屑「知识库 / **study**」、侧边栏分类「**FRONTEND / STUDY**」——
  实拍再次确认 P1-3：原始文件夹名直接外露，应显示「学习笔记 / 前端」。
- 1024px（iPad 横屏）实拍确认 L1：TOC 消失，右侧多出一条死白。

### S7 · 视觉走查的整体判断

把代码推断（L 系列）和实拍（S 系列）合起来看，这个站**视觉语言是统一且高级的**
（星空主题、衬线标题、琥珀点缀、发丝边线都成立），问题不在「风格」而在**布局工程**：
三栏比例、移动端网格、内容稀疏时的兜底。这些恰好都是可以精确修复、不动风格的部分。

---

## 补充 3 · 设计层：主题表达（eafc05e 修复后实拍 v2）

bug 级问题修完后，剩下的「不满意」出在**主题没有贯穿**。这一节不谈 bug，谈设计。
主题设定：「我只知道我一无所知」+ 星空深邃 + 漫天星 = 知识积累。

### D1 · 核心：星图从「控件」升格为「星座」，三个动作 —— ✅ 已实施（见 D2）

主题关键词（用户确认）：**深邃、干净、浩瀚**；且**同系列文章的星必须凝聚在一起**。
（第一版「名言被几百颗暗星围绕」的方案已被否：静态散点显乱，且丢掉了力导向聚类能力。）

**动作一 · 凝聚：启用锚星（组件已内置，零新依赖）**

`BrainGraph` 已实现完整的锚星接口但首页从未使用：
`anchor`（更大更亮的星体）、`color`（域色，恒星色温系）、`count`（篇数）、
锚星名字标签（HTML 投影跟随）、悬停卡片（`desc · N 篇`）、连到锚星的边自动继承域色。

在 `index.astro` 里给每个系列 / 分类生成一个锚星节点（无 url、不可点），
并给每个成员星加一条到锚星的边（权重 ~2）——力导向会把成员星自然拉向锚星，
**系列凝聚成带名字的星座**，文章越多星座越大。这就是「漫天星 = 知识积累」的正确打开方式。

**动作二 · 干净：减层（现在首页同时叠了 5 层星星）**

实拍逐层清点：背景星野 webp、meteors canvas 的 40 颗微星 + 流星、8 颗 `.tw` 闪烁星、
星图内 320 颗远景星点、星图节点 —— 五层星互相叠加，是「乱」的真正来源。
首页只留两层：**星图（主角）+ 星野 webp（远景）**；
`.tw` 和 meteors 在首页关闭（meteors.ts 里按 `document.body.dataset.sky === 'home'` 跳过即可）。

**动作三 · 浩瀚：相机沉浸，而不是远看一个小球**

现在星图是 540px 高的「框」，相机在 z≈11 远看一个 R0=2.7 的小星团 —— 所以显空。
- 画布从 540px 框解放为**整屏**（100vh，hero 文字悬浮其上，见 D2）
- 相机拉近、雾气加重（FogExp2 密度 0.05 → 0.09+），让星从眼前延伸到远处消失
- 远景星野调暗调细（opacity 0.55 → 0.3，尺寸 0.55 → 0.4），只作纵深不作主体
- 星尘边与脉冲密度可以再降一档（干净）

文案上「已点亮 4 颗 · 余下皆是未知」这类围着文字转的表达放弃；
caption 保持极简或干脆去掉。

### D2 · 首屏微调（用户已定调：保留现状主页，只调位置与字体）—— ✅ 已实施（2026-09-26 下午）

按用户定调「能生长 / 不要光污染 / 要深邃」完成实施，实拍验证通过：

- **锚星凝聚**：config 的 SERIES/CATEGORIES 加了 `color` 域色；
  `index.astro` 为系列/分类生成锚星（带名字标签与篇数），成员星以归属边（w=2.2）拉向锚星；
  BrainGraph 每边独立自然长度/劲度（归属边 1.5/0.055）——同系列凝聚成带名字的星座，文章越多星座越大。
- **光污染**：删流光脉冲、星尘亮度 0.5→0.38、锚星边色 ×0.55、远景星野调暗调细（0.55→0.3）、
  雾 0.05→0.062、首页 8 颗 `.tw` 删除、`meteors` 首页关闭。星层从 5 层减到 2 层。
- **排印**：名言放开 max-width（桌面单行、移动端平衡两行，「nothing.」不再悬挂）；
  拉丁原文 12px、间距收紧；星群上移（group.position.y=0.55），消除文字与星之间 ~200px 空带。
- **S2 收口**：移动端裸 `1fr`→`minmax(0,1fr)`（497→405）；
  残余 30px 是行内 code 长路径无断点 → 行内 code 加 `overflow-wrap: anywhere`（→360，彻底消除）。

（历史记录：「hero 悬浮星海」「星座重排」两版概念稿被否后，用户对现有主页结构满意，
最终方案保留了原有星图与布局，只做凝聚、减层、排印三件事。）

### D3 · 「星」的语言没有贯穿到其他页面

除了首页，其他页面完全没有「星」的叙事，只是普通的深色文字页：

- **知识库列表 → 星表**。项目页的 `01` 编号已经有天文台星表的味道，强化它：
  等宽编号（`HIP 001` 式）、日期像观测记录、stageOf 的新星/亮星/恒星**从一行小灰字升级成视觉元素**
  （不同大小/亮度的星点 glyph 放在条目前面，代替现在右上角看不见的 `✦ 新星` 文字徽章）。
- **文章页 h2 分隔线 → 星轨**。现在 `h2 { border-bottom: 1px solid }` 太普通，
  改成一条两端渐隐的细线 + 中央一颗 4px 小星点（纯 CSS 伪元素可做），章节间就有了「星轨」。
- **分类 → 恒星色温**。BrainGraph 注释里已经写了「域色（恒星色温系）」但没落地。
  给 CATEGORIES 每个分类配一个恒星色（学习=蓝白、前端=暖白、项目=琥珀…），
  用在分类标签、侧栏当前项、卡片左边线。全站现在只有琥珀一个 accent，太寂寞；
  恒星色温谱是与主题天然自洽的第二套色彩，不是乱加颜色。

### D4 · 「深邃」= 纵深，目前页面太「平」

全站元素明度挤在一个很窄的区间（背景 #0b0c12、卡片 #12141d、正文 #e8e6df），没有前后层次，
像蒙了一层灰纱。深邃感来自**光的落差**：

- 给页面加**暗角**：`body::before` 之上再叠一层 `radial-gradient(120% 90% at 50% 30%, transparent 40%, rgba(0,0,0,0.5))`，
  中心有光、四周沉入黑。一行 CSS，氛围提升最明显。
- 卡片从半透明改成**更实的深空色 + 更亮的 1px 边**（如 #12141d 实底 + rgba(232,230,223,0.14)），
  让卡片「浮」在星空上，而不是融进去。
- 大标题（h1/hero）加**极淡的光晕**（`text-shadow: 0 0 24px rgba(217,169,32,0.12)`），
  让标题像星一样微微发光 —— 只给最大的标题用，克制。

### D5 · 修复残留（v2 实测仍在）

- **S2 没修干净**：`[...slug].astro` 移动端媒体查询（第 314 行）仍是裸 `1fr`，
  375px 实测 `scrollWidth = 497`，横向溢出 122px 还在。改 `minmax(0, 1fr)`。
- **S3 没修**：移动端全站目录树仍压在正文前。
- 移动端长标题折行后，琥珀句点孤零零挂在最后一行末尾（「…六站对照精读 **.**」），很扎眼。
  建议 `h1::after` 的句点在移动端（或标题超过一行时）去掉。

---

## 建议的执行顺序

**A 组 · 安全与构建（不到 1 小时，建议先做）**

| # | 事项 | 成本 |
| --- | --- | --- |
| 1 | 部署改 SSH key + 专用用户；`server.ini` 密码改环境变量 | 半小时 |
| 2 | build 前清 dist + 上传前校验关键产物 | 10 分钟 |

**B 组 · 布局硬伤（都是几行 CSS，改完立刻看得出差别）**

| # | 事项 | 成本 |
| --- | --- | --- |
| 3 | **S2 移动端网格 `1fr`→`minmax(0,1fr)`，修掉 122px 横向溢出** | 2 分钟 |
| 4 | L1 笔记详情去掉 <1200px 的死白第三列 | 5 分钟 |
| 5 | L2 页头 / 笔记正文容器统一为一条中轴线 | 5 分钟 |
| 6 | L3 卡片网格改 `minmax(min(320px,100%),1fr)` 修窄屏溢出 | 2 分钟 |
| 7 | L4 首页闪烁星错开 delay | 5 分钟 |
| 8 | L5 统一「页眉带 → 正文」垂直节奏 | 20 分钟 |
| 9 | L7 sticky 起止线收成一个变量 | 10 分钟 |
| 10 | L11 专栏卡底部补留白 | 2 分钟 |

**C 组 · 一致性与性能（需要动脑，但不动架构）**

| # | 事项 | 成本 |
| --- | --- | --- |
| 11 | **S1 笔记详情三栏重新分配（正文 45%→60%，TOC 加宽）** | 40 分钟 |
| 12 | 分类名 / 专栏名统一读 config（消灭双写） | 20 分钟 |
| 13 | `og:image` 绝对化 + canonical | 10 分钟 |
| 14 | L6 卡片去 backdrop-filter、加强边界 | 15 分钟 |
| 15 | L8 噪点层下沉到背景层 | 5 分钟 |
| 16 | L9 404 页套 SkyBand | 10 分钟 |
| 17 | 清理死 CSS、合并重复规则 | 30 分钟 |

**D 组 · 有工程量**

| # | 事项 | 成本 |
| --- | --- | --- |
| 18 | S3 移动端目录折叠 / 后置到正文之后 | 1 小时 |
| 19 | three.js 懒加载 + 窄屏降级静态列表（顺带补 a11y 等效列表） | 2 小时 |
| 20 | S4 首页星图瘦身 + 「最近更新」入口（S4+L10 合并做） | 1.5 小时 |
| 21 | 部署改 rsync / 软链版本切换 | 1 小时 |
| 22 | 字体瘦身 + README 补自建部署 + 上 CI | 半天 |

A+B 组约 1.5 小时，全是可立刻验证的改动；其中 **S2 是这次实拍抓出来的真 bug，建议最先改**。
C 组的 S1（三栏重排）是让用户觉得「文章页怪」的主因，40 分钟，做完那一页立刻顺了。
